# public/models

Place your 3D body scan files here.

Supported formats:
- `.glb` / `.gltf` — recommended (binary GLTF)
- `.obj` — wavefront OBJ

These are loaded at runtime via the file upload button.
If you have a pre-scanned mesh, drop it here and reference it directly.
